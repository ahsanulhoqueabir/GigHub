meta {
  name: Create - Missing Fields
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/admin/hero-banners
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
  Content-Type: application/json
}

body:json {
  {
    "title": "",
    "image_url": "",
    "alt_text": ""
  }
}

docs {
  ## Admin — Create Hero Banner — Missing Fields

  Tests creating a hero banner with empty required fields.

  ### Expected Error Response (400)
  ```json
  {
    "success": false,
    "error": "...validation error..."
  }
  ```
}
