meta {
  name: Create - Unauthorized
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/admin/hero-banners
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Test Banner",
    "image_url": "https://example.com/banner.jpg",
    "alt_text": "Test"
  }
}

docs {
  ## Admin — Create Hero Banner — Unauthorized

  Tests creating a hero banner without an auth token.

  ### Expected Error Response (401)
  ```json
  {
    "success": false,
    "error": "Authentication required"
  }
  ```
}
