meta {
  name: Update Profile - Unauthorized
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/api/auth/profile
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Hacker"
  }
}

docs {
  ## Update Profile — Unauthorized

  Tests updating profile without an auth token.

  ### Expected Error Response (401)
  ```json
  {
    "success": false,
    "error": "Authentication required"
  }
  ```
}
