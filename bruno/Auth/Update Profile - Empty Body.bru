meta {
  name: Update Profile - Empty Body
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/api/auth/profile
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
  Content-Type: application/json
}

body:json {
  {}
}

docs {
  ## Update Profile — Empty Body

  Tests updating profile with an empty body (should succeed with no changes).

  ### Success Response (200)
  ```json
  {
    "success": true,
    "message": "Profile updated successfully",
    "data": { ... }
  }
  ```
}
