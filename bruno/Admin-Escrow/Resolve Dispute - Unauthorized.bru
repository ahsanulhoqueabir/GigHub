meta {
  name: Resolve Dispute - Unauthorized
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/admin/escrow/resolve
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "order_id": "{{orderId}}",
    "resolution": "RELEASE"
  }
}

docs {
  ## Admin — Resolve Dispute — Unauthorized

  Tests resolving a dispute without an auth token.

  ### Expected Error Response (401)
  ```json
  {
    "success": false,
    "error": "Authentication required"
  }
  ```
}
