meta {
  name: List - By Job
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/job-proposal?job=JOB_UUID
  body: none
  auth: none
}

params:query {
  job: JOB_UUID
}

docs {
  ## List Job Proposals by Job — Public

  Filters job proposals by job UUID.
  Returns all proposals submitted for a specific job.
  Replace `JOB_UUID` with an actual job ID.
}
