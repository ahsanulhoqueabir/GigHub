meta {
  name: Create - Missing Fields
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/job-proposal
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
}

body:json {
  {
    "description": "Too short"
  }
}

docs {
  ## Create Job Proposal — Missing Fields

  Tests validation when required field `job` is missing and `description` is too short.
  Expected: 400 Bad Request with validation error message.
}
