meta {
  name: Update Config - Unauthorized
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/admin/system-config
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "maintenance_mode": true
  }
}

docs {
  ## Admin — Update System Config — Unauthorized

  Tests updating system config without an auth token.

  ### Expected Error Response (401)
  ```json
  {
    "success": false,
    "error": "Authentication required"
  }
  ```
}
