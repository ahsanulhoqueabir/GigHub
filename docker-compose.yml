services:
  api:
    build: .
    container_name: api
    restart: always
    ports:
      - '3001:3001'
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:8-alpine
    restart: always
    ports:
      - '6379:6379'
