meta {
  name: SSLCommerz Cancel
  type: http
  seq: 13
}

post {
  url: {{base_url}}/payments/sslcommerz/cancel
  body: json
  auth: none
}

body:json {
  {
    "tran_id": "{{tran_id}}",
    "order_id": "{{order_id}}"
  }
}
