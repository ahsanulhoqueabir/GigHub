meta {
  name: Upload Image
  type: http
  seq: 1
}

post {
  url: {{base_url}}/upload/image?folder=gigs
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file()
}
