meta {
  name: Update Order Status
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/orders/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "status": "processing"
  }
}
