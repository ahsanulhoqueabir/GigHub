meta {
  name: Update Gig - Status
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/gigs/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "type": "status",
    "status": "active"
  }
}
