meta {
  name: Status Change
  type: http
  seq: 2
}

patch {
  url: {{base_url}}/jobs/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "type": "status",
    "status": "closed"
  }
}
